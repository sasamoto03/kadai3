<p id="notice"></p>
<div class="container">
  <div class="row">
</div>
<div>
<h1>Editing Book</h1>
<% if @books.errors.any? %>
  <h3><%= @books.errors.count %>errors prohibited this obj from being saved:</h3>
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @books do |f| %>
<h4>Title</h4>
  <%= f.text_field :title, class: "form-group" %>
<h4>Body</h4>
  <%= f.text_area :body, class: "form-group" %><br>
  <div class= "form-group">
  <%= f.submit 'Update Book', class: "btn btn-success" %>
  </div>
  <%= link_to "Show", book_path(@books.id) %>
  |
  <%= link_to "Back", books_path %>
 <% end %>
</div>